/**
 * Feature flags — flip a value to re-enable gated UI in one place.
 * Data fields may still exist in models/sync/export regardless of these flags.
 */

export const FEATURES = {
  // Reserved for future gates. Keep this module so imports stay stable.
}
